# Trident — Progress Log

## 2026-07-31

### Dev environment setup (this machine)
- Python is managed via `uv` (not a system `python` on PATH). PlatformIO installed as an isolated `uv` tool:
  ```
  uv tool install platformio --with pip
  ```
  (`--with pip` is required — PlatformIO shells out to `pip install` for some package extras, and a bare `uv tool install` venv has no `pip` module.)
- `pio` lives at `C:\Users\simon\.local\bin` — not on PATH by default. Prefix commands with:
  ```
  $env:PATH = "C:\Users\simon\.local\bin;$env:PATH"
  ```
- **Must build with native PowerShell, not Git Bash.** pioarduino's toolchain installer (`idf_tools.py`) detects MSYS/MinGW shells and refuses to install (`ERROR: MSys/Mingw is not supported`), leaving `toolchain-xtensa-esp-elf` with only metadata and no compiler binary. Run `pio` from PowerShell/cmd.
- Windows long path support had to be enabled (some pioarduino package paths, e.g. under ESP-Matter/connectedhomeip headers, exceed 260 chars):
  ```
  Set-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Control\FileSystem" -Name "LongPathsEnabled" -Value 1 -Type DWord
  ```
  (admin PowerShell, already done on this machine.)

### Code fixes (committed: `51c81b3`)
- `platformio.ini`: `platform = espressif32` pointed at the official PlatformIO registry, which only resolves to an old Arduino-ESP32 2.0.17-based build — too old for APIs this codebase uses (`rgbLedWrite`, newer `ledcAttach`, BLE `String` overloads). Repinned to the **pioarduino** fork (needed anyway for `env:c6` / ESP32-C6 support, which the official registry never had):
  ```
  platform = https://github.com/pioarduino/platform-espressif32/releases/download/stable/platform-espressif32.zip
  ```
- `src/ble.cpp`: `MyServerCallbacks::onConnect` used the old Bluedroid signature (`esp_ble_gatts_cb_param_t*`). The framework's BLE library is now NimBLE-based, so it was silently not overriding the base callback. Fixed to `onConnect(BLEServer*, ble_gap_conn_desc*)`, using `desc->conn_handle` instead of `param->connect.remote_bda` for `updateConnParams(...)`.

### Verified on real hardware (ESP32-S3 devkitc board, env `s3`)
- Build + flash via `pio run -e s3 -t upload --upload-port COM5` — success.
- **USB (TC4/Artisan protocol)**: sent raw `READ` over the serial port, got back `0, 0.0,0.0,0,0` — matches the expected format in `main.cpp`'s `serialLoop()`. **Not yet tested through the actual Artisan app.**
- **WiFi**: device (no saved credentials) broadcasts open AP `trident` as expected. Connected a laptop to it, confirmed `192.168.4.1` responds to HTTP, and `/api/wifi` returns 400 when called without `ssid`/`pass` params (didn't POST real credentials, to avoid dirtying device state). **Not yet tested through the actual Artisan app.**
- **BLE**: device advertises as `Skywalker-Trident`. HiBean pairing initially got stuck on "connecting" and timed out — turned out to be a HiBean-side issue (need to pick device type "Skywalker HB" in the app, not the generic/"comm" option). Once selected correctly, pairing succeeded and control worked.

### Known non-blocking warnings (not yet cleaned up)
- `BLEServer::updateConnParams()` is deprecated in favor of `requestConnParams()`.
- Several `new BLE2902()` calls in `ble.cpp` are deprecated — NimBLE auto-adds the CCCD descriptor when notify/indicate is enabled, manual `addDescriptor(new BLE2902())` is a no-op now.

### VS Code PlatformIO IDE extension (alternative to CLI)
- Installed the official PlatformIO IDE extension so builds can be done from VS Code instead of asking Claude to run `pio` in a terminal.
- `platformio.ini` now has `[platformio]` / `default_envs = s3` at the top, so both the IDE's default Build/Upload buttons and a bare `pio run` only touch `s3` (skip `c6`/`s3-mini`).
- **Gotcha**: the extension's installer and the `uv`-installed CLI both write into the same `~/.platformio` core dir, and stepped on each other's internal `penv` (pioarduino's helper Python venv used to install packages like `pyyaml`, `cryptography`, `esptool`, etc.). Symptom: `pio run` fails with "Python version mismatch... Recreating penv" then "Error: uv installation via pip failed with exit code 106" (real error is swallowed — pioarduino's `penv_setup.py` redirects it to `DEVNULL`). Root cause: `uv venv --python=<nested tool-venv python>` produces a broken venv with no `pyvenv.cfg` and no pip. Fixed by deleting `~/.platformio/penv` and recreating it directly with the real base interpreter:
  ```
  & "C:\uv-python\cpython-3.14-windows-x86_64-none\python.exe" -m venv "C:\Users\simon\.platformio\penv"
  ```
  If this resurfaces after reinstalling either tool, redo the above rather than fighting the auto-recreate logic.

### ILI9341 display added (committed: `7acbcdd`)
- Hardware: **Goouuu ESP32-S3 expansion board** with a bundled 2.8" **ILI9341** SPI TFT (240×320). Swapped out the unused ST7789 scaffold (`Display_ST7789.*` files left in place but now dead code, not deleted).
- Pins (confirmed working on real hardware): `TFT_MISO=46, TFT_MOSI=45, TFT_SCLK=3, TFT_CS=14, TFT_DC=47, TFT_RST=21` — found via a matching board's public pinout, not from Goouuu's own docs, but verified by flashing.
- `env:s3`'s `-D NO_DISPLAY` flag removed so the display actually compiles in.
- Display shows the same status string as WebSerial (`Status: heater,fan / Wifi: ip`), refreshed every ~250ms from `main.cpp`'s `webSerialLoop()`.
- Iterated through several rendering issues based on live hardware feedback:
  1. Default GFX font at `setTextSize(4)` was too large and looked blocky → switched to the bundled `FreeSans9pt7b` font at size 1 (smoother glyphs).
  2. Redrawing via full-screen `fillScreen()` every update caused visible flicker → tried opaque (fg,bg) text color to skip the clear, but custom GFX fonts (unlike the classic bitmap font) draw with a **transparent background only**, so old and new text piled on top of each other instead.
  3. Tried clearing just a bounding rect (`fillRect`) before redrawing — fixed the overlap but the clear-then-redraw was still visibly flickering.
  4. Final fix: render into an off-screen `GFXcanvas1` (1bpp, ~3.2KB RAM) each update, then `tft.drawBitmap(...)` it to the panel in one shot. No intermediate blank frame is ever shown on the physical screen. **This is the version that's committed and confirmed flicker-free on hardware.**

### Possible upstream bug found while explaining the code (not fixed yet)
- [state_request_queue.cpp](src/state_request_queue.cpp): [model.h:6](src/model.h#L6) comments the source priority as "BLE=0, WS=1, USB=2", implying BLE should win conflicts. But both `enqueueStateRequest`'s eviction logic and `processStateQueue`'s selection logic (`stateQueue[i].source > highestPriority`) pick the **largest** raw enum value — so in practice **USB(2) beats WebSocket(1) beats BLE(0)**, the opposite of what the comment says. This is upstream code (predates today's session, part of the original fork), not something introduced this session. Since this repo was forked from elsewhere, worth checking whether upstream intended BLE to win and the comparison operators are backwards, or the comment is just stale — haven't fixed it, just flagging it.

## 2026-08-01

### Touch panel control added (branch: `touch-panel-control`)
- New XPT2046 resistive touch layer over the ILI9341 screen: on-screen buttons for Fan −/+, Heat −/+, and STOP (`src/touch.h`, `src/touch.cpp`), wired into the existing `enqueueStateRequest()`/`processStateQueue()` arbitration as a new `SOURCE_TOUCH` ([model.h](src/model.h)), except STOP which calls `eStop()` directly, bypassing the queue entirely (deliberate — immediate local emergency stop shouldn't wait on arbitration).
- **Pin discovery**: initially guessed the touch controller shared the display's SPI bus (`TFT_SCLK/MOSI/MISO`) with only its own `TOUCH_CS`, copying the pattern that worked for the display pins. Wrong — the on-screen "touched" indicator stayed permanently true even with no finger on the panel (floating/garbage SPI reads). Found a blog post describing this exact board (its listed TFT pins matched ours exactly, confirming it's the same board) stating the touch controller is wired to a **completely separate SPI bus**: `TOUCH_CS=1, TOUCH_CLK=42, TOUCH_MOSI=2, TOUCH_MISO=41` ([display.h](src/display.h)). Since the `XPT2046_Touchscreen` library always talks to the global `SPI` object (no way to inject a different `SPIClass`), fixed by giving the **display** its own dedicated `SPIClass tftSPI(HSPI)` instead ([display.cpp](src/display.cpp)) and reserving the global `SPI` for touch (`touch.cpp`'s `touchInit()`).
- **Coordinate calibration**: default XPT2046 raw ADC range (200–3900) was a generic placeholder. Measured actual 4-corner raw values on hardware and found the raw X/Y axes are **not swapped** (original code guessed a swap — wrong) but **both run inverted** relative to screen coordinates. Recalibrated `TS_MINX/MAXX/MINY/MAXY` in `touch.cpp` from real corner readings; confirmed via a temporary on-screen raw/mapped-coordinate readout (reused the status text area, since WebSerial requires a WiFi connection mid-hardware-test) that taps on STOP and Fan+ land inside their correct button rectangles.
- **Verified on real hardware**: Fan+ button changes `F:` by the expected step; STOP immediately forces `H:0, F:100` regardless of queue state, bypassing arbitration as designed.
- Emergency-stop UX tweak per user feedback: `BTN_STOP_WIDTH` shrunk from full-width (312) to a more proportionate size — no longer spans the entire screen.

### Watchdog auto-shutdown has likely never actually worked (found while testing touch's watchdog bypass, not fixed)
- The touch feature added a `touchSessionActive` bypass to `itsbeentoolong()` ([SkiCMD.h](src/SkiCMD.h)) so a touch session isn't killed by the 10s inactivity timeout. Testing that bypass (STOP clears the flag, then wait >10s idle, expect auto-shutdown) showed `shutdown()` never fires **regardless of `touchSessionActive`**.
- Root cause: `main.cpp`'s `loop()` calls `handlePIDControl()` unconditionally every iteration ([main.cpp:172](src/main.cpp#L172)), which calls `handleHEAT(...)` every single time whether or not anything actually changed. `handleHEAT()` unconditionally sets `lastEventTime = micros()` ([SkiCMD.h:98](src/SkiCMD.h#L98)) — the same timestamp `itsbeentoolong()` checks against a 10-second threshold. Since the main loop runs continuously (thousands of times/sec), `lastEventTime` is perpetually refreshed to "now," so `itsbeentoolong()`'s `duration > LAST_EVENT_TIMEOUT` can essentially never be true.
- This predates the touch feature and isn't specific to it — the 10-second auto-shutdown watchdog appears to be dead code for **every** interface (BLE/WiFi/USB/touch), not just touch. Same category as the priority-order bug above: inherited from the fork, flagging rather than fixing per current direction.

### Dashboard UI redesign + ROR + Drum toggle
- Reworked the on-screen layout based on live user feedback, several rounds:
  - Button rows changed from 2 buttons each (Fan −/+, Heat −/+) to 4 (adding a `0` and `100` quick-set per row: F0/F−/F+/F100, H0/H−/H+/H100), narrower buttons to fit.
  - Each row's live value (Fan %, Heat %) moved from a separate top tile to sit at the end of its own button row, since the adjacent buttons already give context — freed up the top area for a much bigger Temp readout.
  - Temp switched from the classic scaled bitmap font (blocky at large `setTextSize`) to the bundled `FreeSansBold24pt7b` — natively large and smooth instead of scaled-up-and-blocky. Fan/Heat/ROR values all switched to the same font for visual consistency, per user request.
  - Added a **ROR** (rate of rise, °/min) tile next to Temp. **Confirmed with the user**: the existing Temp reading is bean temp (BT) from the roaster's own NTC (not a new sensor) — ROR is derived from it, no new hardware needed. Implemented in [SkiComms.h](src/SkiComms.h)'s `updateROR()`: each new temp sample's instantaneous slope vs. the previous sample is pushed into a rolling buffer, and the displayed ROR is the **moving average of every instantaneous slope from the trailing 5 seconds** (per user's explicit request — smooths sensor jitter better than a single two-point delta). Shown to 1 decimal place. Not yet validated against a real roast (bench-tested with a static/near-zero temp only).
  - Added a **Drum on/off toggle button** next to STOP (`BTN_DRUM_*` in [display.h](src/display.h), `sendDrum()` in [touch.cpp](src/touch.cpp)) — routes through the same `SOURCE_TOUCH` arbitration as Fan/Heat, unlike STOP. Its fill color (green=on, grey=off) is redrawn every dashboard refresh since it reflects live state, unlike the other buttons which are drawn once at boot.
  - STOP button narrowed and centered (was full screen width).
  - Removed the temporary touch-calibration debug UI (indicator box, touch-point dot, on-screen raw/mapped-coordinate readout) now that touch calibration is confirmed working — see calibration values in `touch.cpp`.
- All changes verified on real hardware via the same live-feedback loop used for the earlier display work.

### Windows froze completely (hard hang, not BSOD) twice during `pio upload`
- Happened twice this session, exact point in the upload unclear. Checked: no duplicate/ghost USB-SERIAL devices in Device Manager, board is on a direct motherboard/laptop USB port (not a hub), CH340 driver is a reasonably current version (3.9.2024.9). Nothing conclusive found.
- Best guess, unconfirmed: ESP32 boards toggle DTR/RTS to reset the chip on upload, which makes the USB device briefly disconnect/reconnect — on some USB 3.0 controllers/drivers this kind of rapid re-enumeration is known to occasionally wedge the whole USB subsystem hard enough to hang the OS. This session did ~20 uploads back-to-back, which may have compounded it. Retried after the checks above and it completed fine, so not blocking, but **worth watching for** — if it recurs, worth trying a different USB port/cable or a BIOS/chipset USB driver update.

### Open items / next steps
- Decide whether to fix the priority-order bug above (flip the comparison, or fix the comment/enum values — need to confirm intended behavior first).
- Decide whether to fix the watchdog bug above (likely needs `handlePIDControl`/`handleHEAT` to stop conflating "recomputed the same value again" with "a real new command arrived").
- Touch panel: all buttons (F0/F−/F+/F100, H0/H−/H+/H100, Drum toggle, STOP) confirmed working on hardware; not yet stress-tested for mis-taps right on button edges.
- ROR's 5s moving-average window/smoothing hasn't been validated against a real roast yet — bench-tested with a static temp only (no real BT movement to smooth).
- Dashboard's TEMP tile is bean temp (BT), read from the roaster's own built-in NTC over the existing serial protocol (`calculateTemp()`/`filtTemp()` in [SkiComms.h](src/SkiComms.h)) — not a new sensor. **MAX31865** (RTD, not MAX31855/thermocouple — corrected 2026-08-01) is the planned next addition, for a *second*, independent temperature: environment/exhaust temp (ET). Deferred, not started. Was going to share the SPI bus with the display (SCK=3, MISO=46) using a new CS pin — worth double-checking against the same "shared vs. dedicated SPI bus" surprise the touch controller had (see above) before assuming that works.
- Test USB and WiFi paths through the real Artisan app (not just raw protocol pokes).
- Test against the actual Skywalker roaster hardware (TX/RX pins) — only tested on a bare ESP32-S3 dev board so far.
- Consider cleaning up the deprecation warnings above.
- README's TODO list still has: LED status signalling, general code cleanup.

## 2026-08-11

### WiFi status now shows the actual IP on the dashboard
- `WiFi:AP`/`WiFi:STA` on the on-screen status line ([display.cpp](src/display.cpp)) used to show only the mode, not the address. `webSerialLoop()` in [main.cpp](src/main.cpp#L131-L138) now builds the string as `AP 192.168.4.1` or `STA <dhcp-ip>` (`WiFi.softAPIP()` / `WiFi.localIP()`), so the IP needed to point Artisan's WebSocket device at the ESP32 is visible without a laptop already on the network.

### Artisan WebSocket device: connected, protocol verified end-to-end, upstream Artisan bug found and worked around
- Set up and tested Artisan's built-in "WebSockets" device type (`ws://<ip>:80/ws`) against [CommandLoop.cpp](src/CommandLoop.cpp)'s `onWsEvent()`. Wrote a throwaway `uv run` Python script (not checked into this repo) speaking the same JSON protocol to validate the firmware side independent of Artisan — `getData` polling and `BurnerVal`/`FanVal`/`Drum`/`Cooling` control all round-tripped correctly and were confirmed to actually drive the hardware (heater raised measured temp, drum motor audibly started/stopped).
- **Found, not fixed**: the USB-serial `CHAN` command gets no reply. `serialLoop()` in [main.cpp:107-127](src/main.cpp#L107-L127) only special-cases `CMDType_READ`/`CMDType_STATE_REQUEST`; `CMDType_CHAN` falls through to `parseAndExecuteCommands()` → `handleCHAN()` ([SkiCMD.h:69-73](src/SkiCMD.h#L69-L73)), which only calls `D_println` (WebSerial, not the physical `Serial`) — so Artisan's USB/TC4 `CHAN;...` handshake never gets an ack over the wire. The BLE path handles this correctly (`ble.cpp:97-101` echoes the ack via `notifyBLEClient`); USB never got the equivalent branch. User asked to hold off on fixing this for now.
- **Upstream Artisan bug, root-caused via their own source** (not fixable from this repo): Artisan's Events "Default Buttons → ON" action is unreliable for a WebSocket device — 0/2 real-hardware attempts fired, with or without added `sleep()`/dummy-`send()` retries in the event's command field. Traced to `artisanlib/canvas.py`'s `OnMonitor()`: for non-Kaleido devices it fires the ON action via `QTimer.singleShot(300, self.runOnEventAction)`, only 100ms after `QTimer.singleShot(200, self.threadserver.createSampleThread)` actually starts the device connection — not enough time for a real WebSocket handshake. `artisanlib/wsport.py`'s `send()` silently drops the message (`return None`, no log, no exception) if `self._write_queue` isn't populated yet, which only happens once the connection fully completes inside `connect()`. OFF/DROP/custom buttons work fine since by the time they're pressed the connection has long been established.
  - Workaround since the reliable fix lives in Artisan's own code, not ours: the drum now starts automatically the moment a WebSocket client connects (`WS_EVT_CONNECT` in [CommandLoop.cpp:18-25](src/CommandLoop.cpp#L18-L25)), matching the "drum already spinning right when you connect" behavior the user had over the old Serial/TC4 path. Trade-off flagged to the user: this fires for **any** WebSocket client that connects, not just a real Artisan roast start (e.g. the throwaway test script above also triggers it).
- Artisan-side config notes for future reference: Ports Configuration → WebSocket tab needs Port `80` (not the `81` default) and Path `ws` (not `WebSocket`); Input node names must match the firmware's JSON keys exactly (`FanVal`/`BurnerVal`, not `VENT`/`HEAT`). Events → Buttons/Sliders "WebSocket Command" action syntax: `send({...})` for **fixed-value** buttons (single braces — Config tab's ON/OFF/CHARGE/DROP/etc.), but `send({{...}})` (doubled braces) for **substituting** actions where `{}` is replaced by a live slider value — mixing these up fails silently (confirmed via Artisan maintainer comment on GitHub issue #702).

### Roaster RX/TX ported to RMT hardware, verified on real hardware
- Replaced the interrupt + blocking `pulseIn()` RX and `delayMicroseconds()` bit-bang TX in [SkiComms.h](src/SkiComms.h) with ESP32's RMT peripheral (hardware-timed pulse capture/generation), ported from the sibling `TEST_SkyCommand_Node32s` project's `rmt-roaster-rx` branch (`src/roaster.cpp`/`.h` there), where it was already validated on real hardware. Motivation: Trident runs WiFi + BLE + WebServer concurrently, which is exactly the kind of interrupt jitter that made the old software-timed RX prone to checksum failures — RMT timing is hardware-generated/captured and isn't affected by CPU-side interrupt load.
- Toggle-able per the source project's own pattern: `_ROASTER_RX_RMT_` / `_ROASTER_TX_RMT_` in [pindef.h](src/pindef.h) — comment either out to fall back to the old software-timed version (both versions are kept side by side behind `#ifdef`, not deleted).
- RX is now non-blocking (`getRoasterMessage()` in [SkiComms.h](src/SkiComms.h) returns immediately if no new frame is ready yet, decoded from an `on_recv_done` callback that re-arms `rmt_receive()` — Arduino's `rmtRead()` wrapper only supports single-shot capture and deadlocks on a continuous stream like the roaster's). `main.cpp`'s `loop()` no longer gates `getRoasterMessage()` on the old `roasterStartFound` interrupt flag when RMT is enabled.
- **Verified on real hardware**: flashed to the board (COM6), watched WebSerial (`http://<ip>/webserial`) for an extended run — every `checksum: X, buf: X, match: 1` line matched, zero `Not valid roaster message.` failures, while the browser's own WebSocket traffic to WebSerial was concurrently exercising WiFi. TX confirmed by sending `OT2;20` then `OT2;0` over WebSocket — fan motor audibly started and stopped.
- **Not yet done**: an actual full roast for long-run stress validation (failsafe triggering, watchdog behavior over an hour+) — same outstanding gate the source project itself hasn't cleared before trusting this in their own main branch.

## 2026-08-12

### CHAN's empty Rx root-caused to timing, not the missing-reply bug fixed yesterday
- Yesterday's fix (USB now replies to `CHAN` at all, via `Serial.println` instead of `D_println`) wasn't enough on its own — re-tested with the real Artisan app and `Rx` was still empty every time, while `READ` kept working. Read Artisan's actual source (`artisanlib/comm.py`'s `ARDUINOTC4temperature()`) instead of guessing: it writes `CHAN;...`, sleeps **only 100ms**, then reads a line; if the result is empty (not merely wrong, just *empty*), no exception is raised but `ArduinoIsInitialized` never gets set, so Artisan silently retries the entire CHAN handshake forever (matches the observed ~2s retry loop exactly). `READ` never "fails" the same way because Artisan has no equivalent validation gating on it — a late/empty reply there just becomes `-1` placeholder data.
- Also found two smaller protocol gaps while reading `comm.py`, not yet fixed: Artisan sends `FILT;<filters>` right after CHAN/UNITS during init (to configure per-channel smoothing) and `PID;CHAN;<source>` (from Artisan's own internal software-PID feature) — neither is handled in [SkiCMD.h](src/SkiCMD.h)'s `parseAndExecuteCommands()`. Both are silently ignored rather than breaking anything (empty replies don't fail Artisan's init check), so low priority, but worth doing if a "full TC4 compliance" pass ever happens.

### Root cause of the 100ms miss: USB serial was polled from the display-refresh loop
- Mapped every task's per-iteration cost. `loop()` (roaster RX/TX/PID/state-queue) is dominated by the RMT TX call's ~78ms blocking wait (`rmt_tx_wait_all_done`, unconditional every iteration → ~12.8Hz) but doesn't touch USB. `webSerialLoop()` (the FreeRTOS task from `xTaskCreate`, no core pinned) is where `serialLoop()` lived, gated behind a **fixed `delay(250)`** that traces back to the project's very first commit (`8bd83b3`, originally `delay(500)`, halved in `4ddff16` for unrelated command-queue work) — it was sized to pace the OLED/TFT status redraw, not USB responsiveness; USB got bolted onto the same loop by coincidence of code organization, not design.
- Cross-checked BLE (`ble.cpp`'s `onWrite`) and WebSocket (`CommandLoop.cpp`'s `onWsEvent`): both already reply synchronously from their own stack's callback (NimBLE's / AsyncTCP's background task), completely independent of `webSerialLoop()`'s 250ms cadence — `bleLoop()`/`webSocketLoop()` only refresh the outgoing-reply state snapshot and do connection housekeeping, they don't receive data. USB serial was the only one of the three still polled once per 250ms+ cycle.

### Tried making USB serial event-driven to match BLE/WebSocket — `Serial.onReceive()` doesn't fire on this toolchain
- First attempt: `Serial.onReceive(onSerialReceive)` calling `handleSerialCommand()` (String parsing + `Serial.println()`) directly in the callback. Broke serial entirely after the first command — traced to the callback running in HardwareSerial's own `uart_event_task`, which gets only a **2KB stack** at the **system's highest FreeRTOS priority** (`configMAX_PRIORITIES - 1`); String-heavy work + `Serial.println()` there is a known-bad combination.
- Second attempt: kept the callback minimal (drain bytes into a line, hand off via `xQueueSend` to a proper task). Still completely broken — confirmed via a WebSerial-tail script (decodes the `WebSerial` library's binary frame format directly over its `/wserial` websocket, since the JS client wasn't accessible headlessly) that `onSerialReceive()` **never fires at all**, with or without anything in it. Cross-referenced against `espressif/arduino-esp32` GitHub issues: `onReceive()` has known problems on ESP32/S2 without the UART clock source set to APB; this build (pioarduino platform, `framework-arduinoespressif32-libs @ 5.5.5`) apparently has the same class of issue on S3. Not worth debugging further into the framework — abandoned event-driven Serial for now.
- **Landed on**: a dedicated FreeRTOS task (`serialCommandTask` in [main.cpp](src/main.cpp)) polling `Serial.available()` every 5ms via `vTaskDelay`, completely decoupled from `webSerialLoop()`'s `delay(250)`. Same read/reply logic as before (`handleSerialCommand()`, factored out so both the old removed `serialLoop()` and this task share it), just on its own fast, independent cadence. Verified via raw serial round-trip (bypassing Artisan): `CHAN` now replies in 4-225ms (previously an unconditional ~250-260ms floor that always lost to Artisan's 100ms budget).
- **Verified against the real Artisan app**: `CHAN;1234` now gets `Rx = # Active channels set to 2100` on the very first try (~100ms after port open), no more endless retry loop. `OT1`/`OT2`/`DRUM` control commands confirmed reflected in subsequent `READ` replies (`90,40` etc.) — full USB/TC4 control loop working end-to-end through Artisan.

### Display drawing also split out of webSerialLoop() into its own task
- Once USB was carved out, realized display drawing was *also* only sharing `webSerialLoop()`'s `delay(250)` by coincidence — same "bolted onto whatever loop already had a delay" pattern, dating back to the same day-one commit. Nothing else left in that loop (`WebSerial.loop()`, `ledControl()`, `webSocketLoop()`/`bleLoop()`) actually needs 250ms pacing: `WebSerial.loop()` is designed for tight-loop calling, `ledControl()` already self-paces off its own `millis()` check, and the WebSocket/BLE tick just refreshes a state snapshot (more frequent = fresher, not a problem).
- Split into a new `displayLoop` task ([main.cpp](src/main.cpp)) that owns the `delay(250)` display-refresh rhythm by itself. `webSerialLoop()`'s own delay dropped to `vTaskDelay(1)` — a minimal scheduler yield, not a pacing mechanism (needed so the task doesn't starve the watchdog, not because anything left in it needs spacing out).

### New finding while testing: ESP-IDF's own error log bleeds into the USB TC4 byte stream
- Raw serial capture during testing caught framework log lines interleaved into TC4 replies: `# Active channels set to 2100\r\n\r\n[ 62098][E][AsyncWebSocket.cpp:436] _queueMessage(): Too ...`. ESP-IDF's `log_e()` (here triggered by `AsyncWebSocket`'s send-queue-full warning) defaults to printing to UART0 — the same physical wire Artisan reads TC4 responses from. Whenever that warning fires (e.g. WebSocket client queue backs up), it corrupts whatever Artisan is trying to parse on USB at that moment. **Not fixed yet** — flagged as the next thing to look at (options: redirect ESP-IDF's default log output away from UART0, or lower the log level so this class of warning doesn't print at all).

### Dashboard now shows link state and protocol-handshake state separately for BLE/USB
- The `BLE:`/`USB:` status letters used to just mean "OK" once a raw connection existed (`deviceConnected` for BLE, "any USB byte in the last 5s" for USB) -- didn't distinguish "something's connected" from "the controlling app (HiBean/Artisan) actually did the CHAN handshake and is really using us." User wanted both states visible, not collapsed into one flag.
- Now shown as 2 characters per interface (same width as the old `OK`/`--`, so the display layout is unchanged): 1st char = link (`C`) or not (`-`), 2nd char = handshake done (`H`) or not (`-`). `--` / `C-` / `CH`.
- [ble.cpp](src/ble.cpp): new `hibeanHandshakeDone`, set on `CMDType_CHAN` in `MyCallbacks::onWrite()`, cleared in `MyServerCallbacks::onDisconnect()` -- BLE has an explicit disconnect event so no timeout heuristic needed there, unlike USB.
- [main.cpp](src/main.cpp): USB link is approximated as "byte seen in the last 5s" (no hardware connect/disconnect signal exists for serial), and `artisanHandshakeDone` is cleared alongside it on timeout so a stale "handshaked" state doesn't linger past when Artisan's actually gone.
- Verified on hardware: `USB:CH` confirmed via a raw-serial CHAN+READ keepalive script (not checked into the repo).
- **BLE tested via a script instead of the real HiBean app**: wrote a throwaway `bleak`-based mock (not checked into the repo) that connects to the Nordic UART Service, sends `CHAN`, then polls `READ` -- no phone needed. Watched the dashboard progress exactly as designed: `--` (nothing) -> `C-` (BLE linked, no handshake yet) -> `CH` (CHAN processed) -> `--` again once the script disconnected (`onDisconnect` clears both flags).

### Extended the same link/handshake split to WebSocket, with a WS: indicator
- WiFi's status only ever showed the radio-link layer (AP/STA mode + IP) -- one layer short of BLE/USB's new link+handshake split, since the layer that actually matters for "is Artisan really talking to us" over WiFi is the **WebSocket connection**, not WiFi itself. WiFi is transport, same tier as the USB cable or the BLE radio.
- Added a `WS:` indicator (same width as WiFi's status line was extended to fit) using the same `--`/`C-`/`CH` scheme: link = `wsClientConnected()` (a new helper in [CommandLoop.cpp](src/CommandLoop.cpp), just `ws.count() > 0` -- no separately-tracked flag needed since `AsyncWebSocket` already knows this authoritatively), handshake = new `wsHandshakeDone`, set the first time a `getData` command is processed in `onWsEvent()`'s `WS_EVT_DATA` case, cleared in `WS_EVT_DISCONNECT` (WebSocket has an explicit disconnect event like BLE, so no timeout heuristic needed).
- `displayDashboard()`'s signature grew a `wsStatus` param ([display.h](src/display.h)/[display.cpp](src/display.cpp)); status line format is now `WiFi:%s WS:%s BLE:%s USB:%s`, buffer bumped 48->64 bytes for headroom.
- Verified on hardware via the existing `artisan_ws_mock.py` test script: `WS:` went `CH` while connected and polling `getData`, back to `--` once the script disconnected.

### ROR recalculated to match Artisan's own algorithm instead of a home-grown one
- The original ROR ([SkiComms.h](src/SkiComms.h)'s `updateROR()`, from the 2026-08-01 dashboard work) averaged a bunch of noisy instantaneous point-to-point slopes over a 5s trailing window -- looked jumpy and didn't read like what Artisan itself would plot from the same temperature stream. User wanted the two to look similar, so read Artisan's actual source (`artisanlib/canvas.py`'s `compute_ror_simple()`) instead of guessing at a design.
- Artisan's real algorithm (confirmed against this project's own `skywalker.aset`, which has `polyfitRoRcalc=false` -- the simple version, not the least-squares polyfit alternative -- and `DeltaSpan=20`/`DeltaETspan=20`): **one slope** between the current reading and a reading ~20 seconds ago, where the *old* end of that slope is locally smoothed (average of nearby samples) but the *current* end is left raw -- smooths noise without adding lag to the newest reading.
- Reimplemented to match: `ROR_SPAN_MS=20000` (from `DeltaSpan`), `ROR_LEFT_SMOOTH_MS=2000` window for the old-end averaging (Artisan does this as a fixed 5-sample average at its own ~2s polling rate from `Delay=2000` in the .aset; since Trident samples much faster via RMT (~114ms), the equivalent is expressed as a time window rather than copying "5 samples" literally -- otherwise it'd smooth over a much narrower real-time span than Artisan's version does). History buffer grew from 20 slots (~2.3s, not even really covering the *old* 5s window it claimed to) to 256 (comfortably covers 20s+ at the RMT sample rate); costs ~3.7KB more RAM, trivial on the S3.
- Known behavior change: ROR now needs a full 20s of history before it shows anything (stays at its last value/0 until then) -- correct per Artisan's own behavior, just slower to first-show than the old 5s-window version.
- Verified on hardware via WebSerial log: `span` consistently 20.0-20.1s, `35-sample left avg` (matches ±2000ms at ~8.75Hz), values smooth and stable at idle instead of jumpy.

### Fixed board profile: platformio.ini was building for the wrong ESP32-S3 flash/PSRAM config
- Checked the actual chip directly (`uvx esptool --port COM9 flash-id`) rather than trusting the board name: real hardware is **ESP32-S3, 16MB flash (quad), 8MB embedded PSRAM (octal)** -- matches the `ESP32-S3-N16R8` reference docs already in `src/`. `platformio.ini`'s `env:s3` was using the generic `esp32-s3-devkitc-1` board profile, which PlatformIO defaults to an 8MB-flash/no-PSRAM variant -- undersizing the flash partition table and leaving the 8MB PSRAM completely undeclared/unusable.
- Fixed by switching to the pioarduino fork's `esp32-s3-devkitc1-n16r8` board definition, which correctly brings in `default_16MB.csv` partitions, `-DBOARD_HAS_PSRAM` + `qio_opi` memory type, and `upload.flash_size=16MB`.
- Verified: build now reports the full ~6.25MB app partition (was ~3.3MB) at the same firmware size, and the board boots/displays normally on real hardware after flashing.
- **PSRAM is enabled but not yet used anywhere in the code** -- nothing currently allocates onto it (`ps_malloc()`/`heap_caps_malloc(MALLOC_CAP_SPIRAM, ...)`), it's just available now for whenever a bigger buffer is needed. Wrote the raw chip-query output into `src/ESP32-S3-N16R8開發板規格.md` for future reference.

### Recurring crash during testing: board goes unresponsive, screen blank/white -- pattern noted but unconfirmed
- Happened **three times** this session, each time shortly after flashing a fresh build and immediately hammering it with a test script (rapid `CHAN`/`READ` over USB while WiFi + BLE advertising + the new `displayLoop` are all also active). Symptoms: USB, WiFi, and WebSerial all stop responding; screen goes black once, white/hung twice. Every time, a manual reset (power cycle) immediately and fully recovered it — no lasting corruption.
- First occurrence was written off as a physical cable/connection issue. It recurring twice more with the same "fresh flash + immediate heavy concurrent load" trigger makes that explanation less convincing — leaning toward a **possible brownout**: WiFi + BLE + display + high-frequency USB serial all being active at once right after boot is a real current-draw spike, and this is a hobbyist board (Goouuu ESP32-S3) without confirmed power regulation headroom. Not confirmed — no crash log was ever captured (Serial is occupied by the TC4 protocol, so no debug UART was free during the actual crash moment).
- **Not fixed, no root cause confirmed.** If this keeps happening: try adding a brief settle delay between flashing and starting heavy test traffic, capture a crash log via the second UART or WebSerial's crash-safe buffer if pioarduino exposes one, or measure supply voltage under combined WiFi+BLE+display+serial load. Worth its own investigation session rather than guessing further here.

### Open items for next session
- Investigate the recurring crash/blank-screen pattern above.
- Fix the ESP-IDF log-to-UART0 contamination issue above.
- Decide whether to implement the `FILT;` and `PID;CHAN;` gaps found in Artisan's protocol.
- Long-run real-roast stress test for the RMT roaster port (still outstanding from yesterday).

## 2026-08-12 (continued) — `lvgl-ui` branch: minimal LVGL bring-up

Evaluated switching the dashboard to LVGL; before rebuilding the whole UI as LVGL widgets, built the smallest possible proof it links, configures, and can push real pixels through the existing `tft` object (`Adafruit_ILI9341`). Verified on real hardware after working through four unrelated bugs surfaced along the way:

- **`lv_conf.h` not found compiling lvgl's own sources.** `include/lv_conf.h` exists and `LV_CONF_INCLUDE_SIMPLE` is set, but `pio run -v` showed the project's `include/` directory never appears in *any* compile command in this build (not even for our own `src/*.cpp` — PlatformIO's documented "include/ is auto-added to every compile unit" didn't hold here, root cause not chased further). Fixed pragmatically: added `-I include` directly to `[env:s3]`'s `build_flags` in `platformio.ini`.
- **`display.cpp` missing `#include "dlog.h"`.** The new LVGL section calls `D_println()` for diagnostics but the file never included the header that defines it — caught immediately by the build (`'D_println' was not declared in this scope`).
- **Race condition between `lvglInit()` and the `displayLoop` task — the real cause of a screen that cycled blue / blue-with-a-white-bar / rendered text, over and over.** `xTaskCreate(displayLoop, ...)` runs in `setup()` *before* `lvglInit()` is called, and `displayLoop`'s task immediately starts looping on `lvglLoop()`. The original code assigned the global `lvglDisplay` pointer as soon as `lv_display_create()` returned — non-null at that point but still mid-configuration (buffers/label not yet set up). The instant `lvglDisplay` went non-null, `displayLoop`'s task started calling `lv_timer_handler()` *concurrently* with `lvglInit()`'s own remaining setup calls, on LVGL internal state that isn't thread-safe. Fixed by building the display into a local `lv_display_t*`, doing all setup (flush cb, buffers, the test label) on that local, and only assigning it to the global `lvglDisplay` as the very last line of `lvglInit()` — `lvglLoop()`'s existing `if (lvglDisplay == nullptr) return;` guard then correctly gates the task until setup is 100% complete.
- **`displayLoop` task stack too small for LVGL.** Sized at `configMINIMAL_STACK_SIZE + 2048` for the old, lightweight `displayDashboard()` direct-drawing code; LVGL's `lv_timer_handler()` (object tree walk, font/glyph rendering) runs much deeper and was overflowing it — bumped to `+8192`.
- **LVGL draw buffers in internal SRAM starved BLE's init.** While isolating the race-condition bug above, temporarily moved the two LVGL draw buffers from PSRAM to static internal-RAM arrays (to remove PSRAM as a variable). That alone broke BLE: `pio device monitor -p COM6 -b 115200 --rts 0 --dtr 0` (see below) showed `BLE_INIT: hci inits failed` / `nimble host init failed` on every single boot, followed immediately by a null-pointer crash (`ble.cpp:133`, `pServer->setCallbacks(...)` on the null `pServer` that `BLEDevice::createServer()` returns after a failed init) and reboot — a fast, silent, ~8-second crash loop that looked from the display alone like an intermittent "flash of blue" (the color `lvglInit()` paints at its very start, before LVGL renders anything) rather than what it actually was. Moved the buffers back to PSRAM (`heap_caps_malloc(..., MALLOC_CAP_SPIRAM)`) — BLE/WiFi need internal RAM specifically for their stacks, LVGL's buffers don't need to be internal, so PSRAM is the correct home for them. `ble.cpp:133` itself still has no null-check on `pServer` (pre-existing latent bug, not touched this session — just now understood as reachable).
- **`pio device monitor` on this board resets straight into bootloader ("waiting for download") on plain connect**, making it useless for catching runtime crash logs by default — its default DTR/RTS toggle on open matches this board's auto-program circuit's bootloader-entry pattern rather than a normal reset. Passive attach with `--rts 0 --dtr 0` (no signal toggling at all) was what actually let boot/crash logs be read.
- Also discovered (unrelated to any bug): `D_println`/`D_print`/`D_printf` ([dlog.h](src/dlog.h)) route to `WebSerial` (the WiFi debug console) only — they never reach `pio device monitor`'s physical USB serial output. Anything needed in a physical-serial crash investigation has to be a plain `Serial.println()`.
- `displayInit()` no longer does the old dashboard's one-time boot-time draw (`drawButtons()` + `displayDashboard(0,...)`) on this branch — screen now starts black and stays whatever LVGL last painted, so there's no leftover old-UI content to visually confuse against genuine LVGL failures. That code is untouched on `main`/`touch-panel-control`; will come back once the dashboard itself gets rebuilt in LVGL widgets.
- **Verified on real hardware (COM6)**: after all five fixes above, "LVGL on Trident" renders and stays stable — confirmed via 25s of passive serial monitoring with zero reboots (previously crashing/rebooting roughly every 8s) and direct visual confirmation on the physical panel. Label currently renders upside-down relative to the panel's physical mounting — `lv_display_set_rotation()` not yet wired up, deferred until the real dashboard gets built in LVGL.

### Open items for `lvgl-ui` branch (superseded below where noted)
- ~~Wire up `lv_display_set_rotation()`~~ — done, see below.
- Rebuild the actual dashboard (temp/ROR/heat/fan tiles, buttons, connection-status line) as LVGL widgets — this session only proved the minimal bring-up.
- ~~Once the dashboard is rebuilt, wire touch as an LVGL input device~~ — the indev itself is done (see below); reconnecting it to real hardware control still waits on the dashboard rebuild.
- Consider fixing `ble.cpp:133`'s null-`pServer` crash defensively (currently only "fixed" by not starving BLE's init of RAM) — a future low-memory situation could hit it again from a different angle.

## 2026-08-12 (continued) — `lvgl-ui` branch: rotation fix + touch wired into LVGL

Continued the minimal-LVGL-bring-up thread: fixed the upside-down rendering, then wired the existing XPT2046 touch panel into LVGL as a proper input device, verified with one interactive widget (a button with a tap counter) rather than assuming coordinates line up. Planned with the user first (touch driving real heater/fan/drum output was explicitly called out as a safety concern before starting).

- **Rotation**: `lv_display_set_rotation(disp, LV_DISPLAY_ROTATION_180)` added in `lvglInit()` ([display.cpp](src/display.cpp)). The flush callback writes straight into `tft.setAddrWindow()`/`writePixels()` with no transform of its own, through the same `tft` object and the same `tft.setRotation(1)` the old (pre-LVGL) dashboard drew through — so it wasn't obvious why LVGL's output came out upside-down. Didn't chase the underlying *why*; LVGL's own SW rotation is the right tool regardless of the cause, and 180° fixed it, confirmed visually on hardware.
- **Old touch dispatch disabled for this branch.** `touch.cpp`'s `touchLoop()` used to read the panel every `loop()` iteration and dispatch straight into `enqueueStateRequest()` (real heater/fan/drum/cooling commands) via `pointInRect()` hit-testing against the old dashboard's button coordinates. Those buttons are no longer drawn on this branch (see last session's entry), but `touchLoop()` was still being called unconditionally — meaning a tap anywhere near an old button's now-invisible coordinates would silently still drive real hardware. `touchLoop()` is now a no-op (body left empty, with the dispatch code — `handleTouch()`/`sendFan()`/etc. — still defined but unused, not deleted) until the real dashboard is rebuilt in LVGL and this either moves to LVGL widget event callbacks or gets reconnected.
- **Extracted `touchGetPoint(int*, int*)`** ([touch.h](src/touch.h)/[touch.cpp](src/touch.cpp)) — the same calibrated raw-read logic that used to be inlined in `touchLoop()`, now level-based (reports "touched right now, and where" every call, no edge-detection) since that's what an LVGL indev read callback wants; LVGL does its own press/release/click edge-detection on top. Kept as the single place owning the touch-controller SPI reads and the existing calibration constants (`TS_MINX`/`MAXX`/`MINY`/`MAXY`, from the 2026-08-01 4-corner calibration — still valid, no recalibration needed).
- **Single-task rule extended to touch, not just LVGL.** `touchLoop()` deliberately does *not* call `touchGetPoint()` either, even just to discard the result — it runs on the main `loop()` task, while the new LVGL indev read callback (which does call `touchGetPoint()`) runs from `displayLoop`'s task via `lv_timer_handler()`. Two tasks both hitting the XPT2046's SPI bus concurrently would be the same class of bug as the `lvglInit()`/`displayLoop` race fixed last session — avoided by making sure exactly one task (`displayLoop`) ever touches the touch controller once LVGL owns it.
- **Setup order fix**: `main.cpp`'s `setup()` called `lvglInit()` before `touchInit()`; the new LVGL indev read callback needs the XPT2046 hardware already begun. Reordered to `displayInit(); touchInit(); lvglInit();`.
- **Touch coordinates needed a manual 180° flip that rotation alone didn't provide.** First hardware test: tapping the visible button did nothing, but tapping the mirror-image point (through the screen center) registered instead. `lv_display_set_rotation()` only transforms what's *rendered* — it does not transform indev input coordinates, at least not in this setup. Fixed in `lvglTouchReadCb()` by point-reflecting `touchGetPoint()`'s raw coordinates through the screen center (`(319-x, 239-y)`) before handing them to LVGL. Confirmed on hardware: tapping the actual button now correctly increments its counter label ("Taps: 1", "Taps: 2", ...).
- **Verified end-to-end on real hardware (COM6)**: label right-side-up, tap-counter button responds accurately to touches at its real screen position, no crashes/reboots (checked via `pio device monitor -p COM6 -b 115200 --rts 0 --dtr 0`, passive attach — see last session's finding on this board's bootloader-on-connect quirk).

### Open items for `lvgl-ui` branch (superseded below where noted)
- ~~Rebuild the actual dashboard as LVGL widgets~~ — done, see below.
- ~~Once the dashboard is rebuilt, reconnect touch to real `enqueueStateRequest()` calls~~ — dashboard is rebuilt, but this round was explicitly scoped as visual/interaction-only (confirmed with user first); reconnecting to real control is still the next step.
- Consider fixing `ble.cpp:133`'s null-`pServer` crash defensively (currently only "fixed" by not starving BLE's init of RAM) — a future low-memory situation could hit it again from a different angle.

## 2026-08-12 (continued) — `lvgl-ui` branch: dashboard rebuilt as real LVGL widgets

User's direction: don't reproduce the old button-grid dashboard in LVGL, make it look like an actual LVGL app -- sliders for Fan/Heat instead of 0/-/+/100 buttons, colored LED indicators instead of `--`/`CH` text, icons on some buttons. Planned with the user first (Chinese-language plan review, iterated on a couple of details -- slide-bar step buttons, before starting). Confirmed scope: visual + interaction only this round, no `enqueueStateRequest()` calls yet from any new widget.

**Correction to last session's entry**: `lv_display_set_rotation(LV_DISPLAY_ROTATION_180)` was NOT the right fix for the upside-down rendering, despite fixing the single test label. It rotates the *entire frame* -- both glyph orientation and every widget's logical position -- and a single *centered* label can't reveal a position bug (center is invariant under a 180-degree flip through the same center). Once this real, multi-widget layout was flashed, positions came out mirrored (top<->bottom, left<->right) even though glyphs read correctly. Removed `lv_display_set_rotation()` entirely and reverted the matching manual 180° touch-coordinate flip in `lvglTouchReadCb()` -- position was never actually wrong (the flush callback has always written straight through `tft.setAddrWindow()` with no transform, via the same `tft.setRotation(1)` the old non-LVGL dashboard drew correctly-positioned content through). With both reverted, the real dashboard rendered with correct positions on hardware. The original upside-down-text symptom is still unexplained, but stopped mattering once position (not glyph orientation) turned out to be the thing that actually needed protecting.

**Layout** ([display.cpp](src/display.cpp)'s `lvglInit()`): Temp/ROR as big (`LV_FONT_MONTSERRAT_40`, added to [lv_conf.h](include/lv_conf.h)) black-background tiles (`createReadoutTile()`, matching the old dashboard's `drawTempTile()`/`drawRorTile()` look rather than LVGL's default light theme) — red/orange text respectively. A WiFi AP/STA IP text line (LED shows link at a glance, but doesn't show the address Artisan/HiBean actually needs). Four status LEDs (`lv_led`, `createStatusCell()`) for WiFi/WS/BLE/USB, replacing the old `--`/`C-`/`CH` text codes: grey = no link, yellow = linked, green = handshake done — same flags as before, just painted instead of stringified. Fan/Heat as `lv_slider` flanked by `[-]`/`[+]` step buttons (`createSliderRow()`, one shared implementation for both rows via a small `SliderCtx` struct instead of duplicating near-identical code). Drum/Cool as icon toggle buttons (`LV_SYMBOL_REFRESH`/`LV_SYMBOL_DOWN`, `LV_OBJ_FLAG_CHECKABLE` — LVGL auto-toggles the checked visual, no callback needed), Stop as a red icon button (`LV_SYMBOL_STOP`) that currently just logs — none of these dispatch to real hardware yet, per the confirmed scope. A `lv_timer_create()`-driven periodic refresh (250ms, matching the old cadence) updates Temp/ROR/LEDs/IP text; sliders seed their initial position from the real current output (`sendBuffer[HEAT_BYTE]`/`[VENT_BYTE]`, extern-declared directly rather than re-`#include`-ing `SkiComms.h`, which would duplicate-define its non-`extern` `sendBuffer` array at link time since `main.cpp` already includes it once).

**Iterating layout on real hardware** (several rounds, each flashed and visually checked):
- Slider knob overlapping the +/- buttons at the 0/100 extremes — the knob is a circle centered on the value position and bulges past the track's own bounds, so a track running edge-to-edge against the buttons wasn't enough clearance. Fixed by shortening the track and widening the gap on both sides.
- Drum/Cool/Stop buttons visibly misaligned — root cause was mundane: an earlier edit updated `drumBtn`'s y/height but not `coolBtn`/`stopBtn`'s matching copy-pasted values. Fixed properly this time via `lv_obj_align_to(coolBtn, drumBtn, LV_ALIGN_OUT_RIGHT_MID, ...)` (and `stopBtn` relative to `coolBtn`) instead of three independent hand-typed positions, so they're structurally guaranteed identical rather than depending on three numbers being kept in sync by hand.
- WiFi IP text and the status LED row overlapping — took three attempts. First attempt used a small fixed gap; still overlapped. Second attempt computed the gap from the label's actual rendered height via `lv_obj_get_height()` after `lv_obj_update_layout()` — this produced almost the same number as the original hand guess (the guess wasn't actually far off), so it *also* still overlapped, meaning the real problem wasn't the gap calculation at all. Turned out to be a different bug entirely: `lv_led`'s default style draws a soft glow/halo bigger than its 14x14 core size, and the LED was placed flush against its container's top edge — the glow's top got clipped by the container's own clip bounds, which visually looked exactly like "the LED is touching the text above it." Fixed by redesigning the status cell from a stacked (LED on top, name below) to a side-by-side (LED left, name right) layout — shorter overall, and centering the LED vertically instead of pinning it to an edge gives the glow headroom for free rather than needing a hand-tuned offset.
- General lesson from this round: when hardware feedback says "still overlapping" after a fix, don't assume the same category of bug (spacing) is still the cause just because the symptom looks the same — the LED glow issue was invisible from source code alone and only obvious once *specifically* investigated as its own hypothesis, not as "the gap needs to be even bigger."

**Verified end-to-end on real hardware (COM6)**: build → flash → `pio device monitor --rts 0 --dtr 0` (confirmed no crash/reboot loop) → visual check, repeated across all iterations above. Final layout: Temp/ROR tiles, WiFi IP text, 4 status LEDs, Fan/Heat sliders with working ±5 step buttons and drag-to-step-5 snapping, Drum/Cool/Stop icon buttons all correctly positioned with no overlaps, confirmed by the user.

### Open items for `lvgl-ui` branch (superseded below where noted)
- ~~Reconnect the dashboard's sliders/toggles to real `enqueueStateRequest()` calls~~ — done, see below. **Not yet verified on real hardware** (built + flashed, but the session paused before testing).
- The original "why does LVGL render upside-down without any rotation set" question from last session is still open (moot for now since position, not glyph orientation, turned out to be what mattered) — could resurface if a future LVGL/font/library version change flips glyph rendering again.
- Consider fixing `ble.cpp:133`'s null-`pServer` crash defensively (currently only "fixed" by not starving BLE's init of RAM) — a future low-memory situation could hit it again from a different angle.

## 2026-08-12 (continued) — `lvgl-ui` branch: dashboard wired to real hardware control (UNVERIFIED)

User asked to connect the rebuilt dashboard's sliders/toggles to real `enqueueStateRequest()` control, superseding the previous round's "visual/interaction only" scope. **Built and flashed to COM6, but the session paused for a break right after flashing -- none of this has actually been exercised on real hardware yet.** Next session should start by testing this (drag sliders, tap +/-/Drum/Cool/Stop, confirm the real heater/fan/drum respond correctly) before trusting it or building anything further on top of it.

- Reused the *existing* dispatch functions from the old (pre-LVGL) touch dashboard rather than reimplementing: `sendFan()`/`sendHeat()`/`sendDrum()`/`sendCool()`/`sendStop()` in [touch.cpp](src/touch.cpp) already did exactly this (build a `StateRequestT`, `enqueueStateRequest(req, SOURCE_TOUCH)`, set/clear `touchSessionActive`) -- they were just sitting unused (dormant, per two sessions ago) inside an anonymous namespace. Moved them to file scope and declared them in [touch.h](src/touch.h) so `display.cpp` can call them directly.
- **Fan/Heat**: `SliderCtx` grew a `void (*sendValue)(int)` function pointer (set to `sendFan`/`sendHeat` per row in `createSliderRow()`), called from `sliderChangedCb()`/`sliderMinusCb()`/`sliderPlusCb()` after each value update (drag *or* button tap) -- every change sends immediately, matching how the old +/- buttons always worked and how a live hardware level control should feel. Initial slider position is still just seeded from `sendBuffer` (read-only) at widget-creation time, not sent.
- **Drum/Cool**: added `LV_EVENT_VALUE_CHANGED` callbacks (`drumToggleCb`/`coolToggleCb`) that weren't needed in the visual-only round -- read back `lv_obj_has_state(btn, LV_STATE_CHECKED)` (LVGL already toggled it) and call `sendDrum()`/`sendCool()` with 100/0, same on/off semantics the old `pointInRect()` dispatch used.
- **Stop**: `lvglStopBtnCb()` now calls `sendStop()` (real ESTOP: heater off, vent full open) instead of only logging -- leaving Stop non-functional while every other control went live would itself have been a safety regression (no working emergency stop on a screen that can drive the heater).
- `touchSessionActive` semantics unchanged from the old touch dashboard: set true by any of the send functions above, only cleared by `sendStop()` -- `itsbeentoolong()`'s inactivity watchdog bypass while true is pre-existing behavior, not something introduced this round.

## 2026-08-12 (continued) — `lvgl-ui` branch: dashboard was one-way, plus a long BLE/HiBean detour

User tested with the real Artisan app over both USB and WebSocket -- both confirmed working end-to-end. But driving Fan/Heat through Artisan didn't move the LVGL dashboard's sliders at all.

### Dashboard was write-only: fixed by syncing from `sendBuffer` on every refresh tick
Root cause: the dashboard's Fan/Heat sliders and Drum/Cool toggles only ever pushed local UI changes out via `sendFan()`/etc (see previous entry) -- nothing ever pulled the *current real* output back in, so a command arriving from USB/WebSocket/BLE changed `sendBuffer` but the on-screen widgets just sat at whatever a local touch last left them at (or their initial boot-time value).

Fixed by extending `lvglRefreshCb()` (the existing 250ms `lv_timer_create()` timer) to also sync Fan/Heat slider position + label and Drum/Cool checked state from `sendBuffer` every tick -- guarded with `lv_obj_has_state(slider/btn, LV_STATE_PRESSED)` so this doesn't yank a widget out from under someone actively dragging/tapping it right now. `drumBtn`/`coolBtn` (previously `lvglInit()`-local variables) promoted to static globals alongside the other status widgets so the refresh callback can reach them; `lvglRefreshCb()` itself got moved later in the file (after `SliderCtx`/`createSliderRow()`/the toggle callbacks) since it now needs to see those symbols.

### Long detour: HiBean BLE compatibility investigation
User separately reported HiBean's "Skywalker HB" device mode connects and controls fine (fan/heat responsive) but never displays temperature, and "Skywalker Comm" mode can't connect at all. Spent a long stretch chasing both. Key context: **"Skywalker HB" is a private company's own app-side integration; "Skywalker Comm" is what the open-source/community side uses** -- two different client implementations talking to the same firmware, per the user.

**Tools built for this** (all in the session scratchpad, not checked into the repo):
- `ble_read_test.py` -- a `bleak`-based script acting as a BLE *client* (mimicking Artisan), connects and sends CHAN/READ directly, to check the firmware's BLE responses independent of HiBean's own app.
- `webserial_ble_tail.py` -- decodes WebSerial's binary frame protocol directly over its raw `/wserial` websocket (own reimplementation, matching the format documented in [[webserial_is_debug_console]]), filtering for a keyword, since the browser's own WebSerial page auto-scrolls too fast to read manually. Needed `ping_interval=None` on the websocket connection -- `ESPAsyncWebServer` doesn't answer this library's default WS-protocol-level keepalive pings, so leaving it on made the client itself decide the connection was dead mid-test.
- `ble_peripheral_sim.py` -- a genuinely different kind of tool: a BLE **peripheral/GATT server simulator running on Windows** (via `winrt` Python bindings for `Windows.Devices.Bluetooth.GenericAttributeProfile`), not a client. Advertises the exact same Nordic UART Service the ESP32 uses, so HiBean's scanners can be pointed at a completely different BLE stack (Windows', not the ESP32's NimBLE) implementing the identical protocol, to separate "is this a protocol problem" from "is this an ESP32/NimBLE-specific problem." Notable API gotchas: needs `winrt-Windows.Foundation` and `winrt-Windows.Foundation.Collections` as explicit extra dependencies beyond the obvious Bluetooth package (lazy-loaded submodules its exceptions/properties depend on); Windows refuses to let an app register a `GattServiceProvider` for `0x180A` (Device Information Service) -- reserved for the OS's own system-level GATT server; WinRT event callbacks (`add_write_requested` etc.) fire on a background COM thread with no asyncio event loop, so calling back into async code from them needs `asyncio.run_coroutine_threadsafe(coro, MAIN_LOOP)` (captured via `asyncio.get_running_loop()` at the top of `main()`), not `asyncio.ensure_future()`.
- `parse_btsnoop.py` -- a from-scratch minimal parser for Android's HCI snoop log format (standard `btsnoop` framing, confirmed via hex dump despite Android naming the bugreport-extracted copy `btsnooz_hci.log`), decoding just enough HCI/ATT to answer "what actually happened during this connection": LE (Enhanced) Connection Complete, Disconnection Complete (with reason codes), and every ATT-layer request/response/notification on a given connection handle within a given record-index range (handle numbers get reused across separate connect/disconnect cycles, so range-bounding matters). Retrieved via `adb bugreport` (installed platform-tools via `winget install Google.PlatformTools`) rather than pulling the raw log file directly -- regular `adb shell` has no permission to read `/data/misc/bluetooth/logs/` even with USB debugging enabled; `adb bugreport` works because Android's bugreport service collects it internally with elevated privilege and hands back a zip. No root, no Wireshark install needed.

**Findings, roughly in the order they were reached:**
1. Firmware's BLE READ response was confirmed byte-correct early on via `ble_read_test.py` (bypassing HiBean entirely) -- CHAN handshake and live temperature both came back correctly formatted.
2. **HiBean "Skywalker HB" mode never sends `READ` or `CHAN` at all** (confirmed via `webserial_ble_tail.py` capturing real traffic) -- only control commands (`PID;SV`, `FILTER`, `OT2`, ...). Added a periodic (1/sec) *unsolicited* notify from [ble.cpp](src/ble.cpp)'s `bleTick()`, gated on `deviceConnected` rather than `hibeanHandshakeDone` (that flag needs CHAN, which also never arrives) -- didn't fix the missing temperature display by itself, but is harmless and arguably more correct regardless (HB mode looks push-oriented, not poll-oriented).
3. Tried matching two different reference HiBean-compatible firmwares already present in this repo (`HiBean/SkiBeanQuickSV/SkiBLE.h` and `HiBean/ESP32S3_Zero_Artisan_HiBean_Roaster_Control_v1.63.ino`) on message formatting (`"0,X.X,..."` vs `"0, X.X,..."`, `\n` vs `\r\n`) and BLE device identity strings (`sketchName`/`firmwareVersion`, tried matching the more complete Artisan+HiBean reference's exact strings) -- **none of this moved the needle**; documented as ruled out rather than left ambiguous. The two reference firmwares disagree with each other on line-ending, which is itself evidence it doesn't matter to HiBean's parser.
4. Added `onRead()` GATT-read logging (`LoggingReadCallbacks` in ble.cpp) to close a real blind spot: `onWrite()` already logged every command, but characteristic *reads* had zero visibility. Confirmed HiBean HB mode never reads any GATT characteristic either (ruling out a device-identity gating theory).
5. **The actual, confirmed, previously-unknown bug**: `initBLE()` never called `pAdvertising->addServiceUUID(SERVICE_UUID)` -- the ESP32's advertising packet only ever contained Flags + device name, no service UUID at all (confirmed directly via nRF Connect's raw advertising-data view on the phone). Discovered by advertising the *identical* NUS service from the Windows peripheral simulator and finding it reliably visible/connectable to "Skywalker Comm" mode's scanner while the ESP32 wasn't -- Comm mode most likely scans with a service-UUID filter (the standard, battery-efficient way to do BLE central scanning), so the ESP32 was never even a candidate. **None of the three reference firmwares in this repo call `addServiceUUID()` either** -- they were evidently only ever validated against "Skywalker HB" mode. Fixed in `initBLE()`.
6. After the fix, the ESP32 *does* now show up in "Skywalker Comm" mode's device list (real progress, confirmed on hardware) -- but tapping it still fails ("connects then hangs/times out" from the user's side).
7. HCI snoop log analysis of an actual failed attempt (see tools above) showed something surprising: **the connection, GATT discovery, MTU exchange, CCCD writes (enabling notify), and a `CHAN` write all completed successfully** -- and a real temperature notification (`"0,33...."`) was delivered and visible in the capture. Then, milliseconds later, the *phone* voluntarily disconnected (`HCI Disconnection Complete: reason=CONNECTION_TERMINATED_BY_LOCAL_HOST`) -- not a timeout, not a remote/firmware-side error. Checked whether this was a "verify once then remember as paired" pattern (common for BLE provisioning flows) by asking the user to check HiBean's device list afterward -- **it wasn't there either**, ruling that out too. What HiBean's Comm mode is actually deciding after a fully successful protocol exchange is closed-source, undocumented business logic this investigation could not get past.
8. **Decision (discussed with user): pause "Skywalker Comm" compatibility here.** The `addServiceUUID()` fix is real, confirmed, and worth keeping regardless of Comm mode's outcome. Getting further would need either official HiBean protocol documentation or a much larger capture/trial-and-error effort with uncertain payoff.

### Confirmed still working
USB and WebSocket paths to real Artisan, tested directly by the user this session -- unaffected by any of the above.

### Open items for `lvgl-ui` branch (superseded below where noted)
- ~~"Skywalker Comm" BLE mode: connects and completes a full successful protocol exchange but the app still doesn't recognize/keep the device~~ — **RESOLVED, see below.**
- The dashboard's touch-driven Fan/Heat/Drum/Cool/Stop controls (this session's "wired to real hardware" change, two entries up) were never directly exercised by touching the physical screen this session -- all real-hardware verification so far went through Artisan (USB/WebSocket), which exercises `enqueueStateRequest()` from a different source (`SOURCE_USB`/`SOURCE_WEBSOCKET`, not `SOURCE_TOUCH`) and confirms the sync-back fix, but not the LVGL widgets' own outbound dispatch.
- Consider fixing `ble.cpp:133`'s null-`pServer` crash defensively (currently only "fixed" by not starving BLE's init of RAM) -- a future low-memory situation could hit it again from a different angle. (Line number may have shifted after the NimBLE-Arduino rewrite below -- re-check.)

## 2026-08-12 (continued) — `lvgl-ui` branch: "Skywalker Comm" BLE, RESOLVED

Picked back up right after pausing (previous entry). User pointed at the actual upstream repo for "Skywalker Comm": **github.com/MagnmCI/SkiBeanCommunity** -- "HiBean Community edition firmware for esp32 control of iTop Skywalker v1 Roasters," GPLv3, explicitly the real source HiBean's Comm-mode client is built to talk to. (Also checked `HiBean/Cross/README.MD`, opened by the user at the same time -- just release binaries + setup instructions, no source, not useful here. Also confirmed with the user that Trident does target Skywalker v1 hardware, ruling out a v1-vs-v2 protocol generation mismatch as an explanation for anything in this investigation.)

Fetched the real current source via `gh api repos/MagnmCI/SkiBeanCommunity/contents/...` (`lib/SkiBLE.h`, `lib/SkiPIDConfig.h`, `src/SkiBeanComm.ino`). Three concrete differences from every reference this session had looked at so far (including the ones vendored into this repo's own `HiBean/` folder):

1. **Uses the standalone `NimBLE-Arduino` library (`#include <NimBLEDevice.h>`, h2zero) — not the ESP32 Arduino core's own bundled BLE classes** (`BLEDevice.h`/`BLEServer.h`/etc, what `ble.cpp` had always used, including through the whole investigation above). Both ultimately wrap the NimBLE stack but are separate libraries with different defaults -- e.g. this official firmware's `initBLE()` never calls `addServiceUUID()` either, yet (per the user's testing) its advertisements are found by Comm mode's scanner just fine. Likely explanation: `NimBLE-Arduino`'s `NimBLEAdvertising` includes registered services' UUIDs by default; the ESP32 core's own `BLEAdvertising` (what was being used) does not, which is why the earlier `addServiceUUID()` fix was needed to get discovery working at all with that library.
2. **A whole additional BLE-only PID configuration interface this firmware never had**: four extra characteristics (`PID_TUNE`, `PID_MODE`, `PID_SAMPLE_TIME`, `PID_MAX_POWER`, UUIDs under a `6dbf02xx-758d-4b5e-bc11-40cfaea42dfe` base), each read/write, each with a descriptor labeling its purpose. The official README explains why: "TC4 doesn't support a complete set of PID commands, and there is no option to read current state over TC4, only write." Comm mode most likely relies on these existing (maybe even checks for them during its own device-compatibility validation) as much as on the core NUS characteristics.
3. **A deliberate `delay(30)` before every notify**, with the comment "Give up time so hibean sees delta between write and notify timestamps" -- HiBean's client apparently associates a write's response with the following notify partly by timing; sending them back-to-back with no gap risks looking simultaneous to it.

**Decision**: given the size of the gap, did a full library swap rather than patching pieces onto the existing implementation (chosen deliberately by the user over the smaller/safer "just add the missing pieces to the existing library" option, accepting the larger blast radius for 1:1 parity with the real reference). [ble.h](src/ble.h)'s public interface (`initBLE()`/`bleTick()`, both plain `String`/`StateRequestT` signatures) doesn't leak any BLE-library-specific types, so the whole rewrite stayed contained to [ble.cpp](src/ble.cpp) -- nothing in `main.cpp`/`touch.cpp`/`display.cpp` needed to change (they only see `deviceConnected`/`hibeanHandshakeDone` as plain `bool` externs).

**What changed in `ble.cpp`**:
- `BLEServer`/`BLECharacteristic`/`BLEService`/`BLEAdvertising`/`BLE2902` → `NimBLEServer`/`NimBLECharacteristic`/`NimBLEService`/`NimBLEAdvertising` (no manual CCCD/`BLE2902` descriptor needed -- NimBLE-Arduino auto-creates it for any `NOTIFY` characteristic). `BLECharacteristic::PROPERTY_*` → `NIMBLE_PROPERTY::*`.
- Server/characteristic callback signatures changed (`NimBLEServerCallbacks`/`NimBLECharacteristicCallbacks`, `NimBLEConnInfo&` replacing the old `ble_gap_conn_desc*`/implicit-connection-context style).
- Added the four PID characteristics, wired directly to Trident's *existing* PID globals (`Kp`/`Ki`/`Kd`/`pMode`/`pSampleTime`/`myPID`, already `extern`-shared with `SkiCMD.h`'s TC4-text PID commands in `main.cpp`) rather than introducing the official firmware's separate `PIDConfig` object -- Trident already has one source of truth for this state; a second one would just be something to keep in sync by hand. `PID_MAX_POWER` needed a small new local `static int pidMaxPower` in `ble.cpp` since `PID_v1` has no getter for its own output-limit ceiling (seeded to 95 to match `main.cpp`'s own initial `SetOutputLimits(0, 95)` call -- the two aren't otherwise linked, so if that default ever changes both need updating).
- Added the `delay(30)` inside the shared `notifyBLEClient()` (applies uniformly to command responses and this session's earlier periodic-auto-notify addition alike -- 30ms is negligible either way).
- **Deliberately did not add `addServiceUUID()` back** -- betting on `NimBLE-Arduino`'s different default matching official behavior. Confirmed correct on hardware (below); noted in a comment as an easy, known-working fallback if a future library version ever needs it after all.
- `platformio.ini`: added `h2zero/NimBLE-Arduino @ ^2.2.3` to `[core]`'s `lib_deps` (shared across all environments, since `ble.cpp` isn't gated by `NO_DISPLAY` or per-board `#ifdef`s).

**Verified on real hardware (COM6)**, in order: build succeeded first try; no crash/reboot loop (`pio device monitor --rts 0 --dtr 0`); LVGL dashboard still renders correctly; **"Skywalker HB" mode still connects and controls fan/heat** (temperature still doesn't display there -- unchanged, still unexplained, now clearly a separate issue from Comm mode's); and finally, **"Skywalker Comm" mode now connects, shows live temperature, and controls the roaster** -- the entire multi-session investigation's actual goal, resolved.

### Open items for `lvgl-ui` branch
- "Skywalker HB" mode's missing temperature display is still unresolved and now clearly independent of the Comm-mode issue (same firmware, same NimBLE-Arduino swap, Comm mode works and HB mode still doesn't show temp) -- low priority given Comm mode (the community/open path) now fully works; HiBean's own private "HB" client-side behavior is equally opaque as Comm's was.
- The dashboard's touch-driven Fan/Heat/Drum/Cool/Stop controls have still never been directly exercised by touching the physical screen this session -- all verification so far has gone through Artisan/HiBean, not the LVGL widgets' own outbound dispatch.
- Consider fixing `ble.cpp`'s null-`pServer` crash defensively -- re-check whether it's even still reachable the same way after the NimBLE-Arduino rewrite (different server/characteristic construction order).

## 2026-08-12 (continued) — `lvgl-ui` branch: scrollbar fix, layout shrink, 3-screen restructure

**Scrollbar fix**: a real scrollbar appeared on the right edge of the dashboard. Root cause: `drumBtn` was 22px tall while `coolBtn`/`stopBtn` were 26px; since the latter two are positioned via `lv_obj_align_to(..., LV_ALIGN_OUT_RIGHT_MID, ...)` relative to `drumBtn`, the taller pair extended 2px past the 240px panel height, and LVGL's screen is scrollable by default. Fixed by unifying all three button heights. Confirmed on hardware: scrollbar gone.

**Layout shrink/enlarge per feedback**: status-LED row cell shrunk further (70x30 → 70x22 → 64x16 → 60x14; LED 10x10 → 8x8) to free vertical space; Drum/Cool/Stop buttons enlarged in the space freed (22px → 34px tall, gap between them 8px → 12px), row moved from y=218 to y=204 so it still fits inside the 240px panel height. Also added `lv_obj_clear_flag(screen, LV_OBJ_FLAG_SCROLLABLE)` right after indev creation as a defensive belt-and-braces measure against any future off-by-a-few-px overflow going unnoticed as "harmless" scrolling.

**3-screen restructuring** (user's explicit design: Splash → Main with Config button → Config with Back button, done for safety reasons -- a live control surface like STOP should never require scrolling/navigating during an active roast, so low-frequency-change info was moved off Main entirely):
- `lvglInit()` now builds three separate `lv_obj_t*` screens via `lv_obj_create(NULL)` (`splashScreen`, `mainScreen`, `configScreen`) instead of building everything onto whatever screen `lv_init()` happened to make active. The original default screen is deleted once the three real ones exist.
- All existing dashboard widgets (Temp/ROR tiles, status LEDs, Fan/Heat sliders, Drum/Cool/Stop) reparented from `lv_screen_active()` to `mainScreen` explicitly. `createSliderRow()`'s signature grew a `lv_obj_t *parent` parameter for this.
- The WiFi AP/STA IP label moved off Main and onto `configScreen`, alongside a new `bleNameLabel` (refreshed every 250ms tick from `getBleDeviceName()`, since `initBLE()` -- which sets the real advertised name -- runs several seconds after `lvglInit()` in `main.cpp`'s `setup()`, so a one-time label would show stale/empty text) and a Back button (`lv_screen_load(mainScreen)`).
- A small Config button was added to `mainScreen` in the space freed by removing the IP label (`lv_screen_load(configScreen)`).
- `splashScreen` is deliberately simple/temporary per the user's request: a centered "Trident" label, auto-advancing to `mainScreen` after 1.5s via a one-shot `lv_timer` (`lv_timer_set_repeat_count(timer, 1)`).
- `lvglInit()` now ends with `lv_screen_load(splashScreen)` instead of leaving whatever screen was last active during widget creation.

**Verified on real hardware (COM6)**: build succeeded first try; reset via RTS-toggle + passive serial capture shows a clean boot log (`[LVGL] init start` → `main screen built` → `config screen built` → `splash screen built` → `init complete`) with no crash/exception text, and no further output for 25s afterward (no reboot loop). **Visual confirmation of the actual splash→main transition, Config button, Config screen contents, and Back button on the physical screen is still pending** -- boot-log-only verification so far.

### Open items for `lvgl-ui` branch
- Visually confirm on the physical device: splash screen shows then auto-transitions to Main after ~1.5s; Main's Config button opens Config; Config shows the real WiFi IP and BLE device name; Config's Back button returns to Main.
- The dashboard's touch-driven Fan/Heat/Drum/Cool/Stop controls have still never been directly exercised by touching the physical screen -- all verification so far has gone through Artisan/HiBean, not the LVGL widgets' own outbound dispatch.
- "Skywalker HB" mode's missing temperature display is still unresolved (unchanged from above).
- Consider fixing `ble.cpp`'s null-`pServer` crash defensively (unchanged from above).

## 2026-08-12 (continued) — `lvgl-ui` branch: Config button relocated, rows reordered, 4-temp layout

**Config button moved into the status row** (user: "Config button 放在跟燈號同一列最尾巴。只要 ICOＮ　不要文字"): pulled out of its own row and placed at the tail end of the status-LED row instead, icon-only (`LV_SYMBOL_SETTINGS`, no "Config" text) since a 14px-tall row has no room for both an icon and a label. Status cells' spacing tightened from 80px to 70px (cell width unchanged at 60px) to free ~40px at the row's right edge for a small 34x20 icon button.

**Rows reordered + temp readouts expanded to 4 channels** (user: "把　燈號和 Cofig button　放到最上面一列。第二列才是溫度. 溫度現在只有 BT , BT_ROR, 請預留 ET , ET_ROR 共4個溫度顯示"): status LEDs + Config now sit in row 1 (top of screen, y=4) instead of below the temp readouts; the temp readouts moved down into row 2 (y=26) and grew from 2 tiles to 4 — **BT, BT RoR, ET, ET RoR** — laid out as four equal 77px-wide tiles across the full 320px width. Only BT/BT RoR are wired to real data (this board only reads one temperature channel); **ET/ET RoR are reserved placeholders showing a permanent "--.-"**, added purely to hold the layout slot for a future second (environment) temperature channel per the user's explicit request — no data source exists for them yet.
- `createReadoutTile()` gained a `caption` parameter and a small caption label (default 14pt font, e.g. "BT", "ET RoR") above the value — with 2 tiles, color alone (red=BT, orange=RoR) was enough to tell them apart; with 4, it isn't.
- Font for the tile values dropped from `montserrat_40` to `montserrat_24` (newly enabled in `lv_conf.h`) since 4 tiles across 320px leaves only ~77px each, nowhere near enough for the old 40pt digits.
- Fan/Heat slider rows and the Drum/Cool/Stop row all shifted down to make room (Fan 118→94, Heat 168→148, bottom row 204→188) — still comfortably fits under the 240px panel height (bottom row now ends at y=222, 18px of margin left at the bottom, more headroom than before this change).

**Verified on hardware (COM6)**: build succeeded (lvgl lib fully rebuilt since `lv_conf.h` changed); reset-and-capture shows a clean boot log through all three screens' construction, no crash/exception, no reboot loop. **Visual confirmation of the new layout on the physical screen is still pending.**

### Open items for `lvgl-ui` branch
- Visually confirm on the physical device: status row + Config icon on top, 4-tile temp row (BT/BT RoR live, ET/ET RoR showing placeholder "--.-") below it, nothing clipped/overlapping, Config icon still taps through correctly from its new position.
- Everything else unchanged from the entries above (splash/Config screen visual confirmation, touch-driven dispatch, HB mode temp display, `ble.cpp` null-`pServer` defensive check).

## 2026-08-12 (continued) — `lvgl-ui` branch: shared left/right margin, wider Fan/Heat sliders

User: "第一列跟第二列左右，要留邊 FAN HEAT 右邊要靠邊一點" -- rows 1 and 2 (status/Config row, temp-tile row) ran flush edge-to-edge with no margin, while the Fan/Heat slider rows stopped well short of the right edge (plusBtn ended at x=292 on a 320px screen, a 28px dead gap) — visually inconsistent with each other.

Standardized on a **4px left/right margin** shared by every row:
- Status row: cells shifted from x=0/70/140/210 to x=4/74/144/214 (still 60px wide, 70px spacing); Config button unchanged at x=282 (already ending at x=316, i.e. already had the 4px right margin).
- Temp-tile row: shifted from x=0/81/162/243 (77px tiles, touching both edges) to x=4/83/162/241 (75px tiles, 4px gaps, 4px margin both sides).
- Fan/Heat slider rows (`createSliderRow()`): minusBtn x=6→4, slider x=54→44 and widened 190px→232px, plusBtn x=260→284 (now ends at x=316, matching the shared 4px right margin instead of leaving a 28px gap), row label x=8→4. The wider slider is a free side benefit of closing that gap -- more usable drag width for the same 0-100 range.

**Verified on hardware (COM6)**: build succeeded; reset-and-capture shows a clean boot log through all three screens' construction, no crash/exception, no reboot loop. **Visual confirmation of the actual margins/slider width on the physical screen is still pending.**

### Open items for `lvgl-ui` branch
- Visually confirm on the physical device: consistent left/right margin across all rows, Fan/Heat sliders now extend closer to the right edge, nothing clipped.
- Everything else unchanged from the entries above.

## 2026-08-12 (continued) — `lvgl-ui` branch: slider knob overlapping +/- buttons at 0/100

User reported ("slide bar 0/100 時 原點疊到 button"): at the slider's extremes, the round knob visually overlapped the -/+ buttons. Root cause: the previous margin-standardization pass tightened the gap between the slider track and its flanking buttons from the original 16px down to 8px (to reach the new shared 4px screen margin) -- not enough clearance for the knob, which draws as a circle bulging past the track's own bounds at 0/100 (the exact failure mode the original code comment already warned about, just not re-checked against the new numbers).

**Fix**: kept the wider slider (bigger drag area is still a net improvement over the original 190px) but restored the proven 16px gap on both sides instead of 8px: slider width 232px→216px, x 44→52 (minusBtn end at x=36, gap 16 to slider start at 52; slider ends at 268, gap 16 to plusBtn at x=284 — plusBtn position itself unchanged, still flush with the shared 4px right margin).

**Verified on hardware (COM6)**: build succeeded; reset-and-capture shows a clean boot log, no crash/exception, no reboot loop. **Visual confirmation that the knob no longer overlaps the buttons at 0/100 is still pending.**

### Open items for `lvgl-ui` branch
- Visually confirm on the physical device: drag each slider to 0 and to 100, knob should sit clear of both buttons at both extremes.
- Everything else unchanged from the entries above.

## 2026-08-12 (continued) — `lvgl-ui` branch: selectable dark/light control-page theme

User: "控制頁面的底色。做成深色跟淺色可選擇。選擇控制頁面底色做在 config 頁面" -- the control page's (Main dashboard's) background should be selectable between dark and light, with the selector living on the Config page.

Implemented via LVGL's own built-in default theme's light/dark variant (`lv_theme_default_init(disp, primary, secondary, dark, font)`), not a hand-rolled color scheme:
- `applyTheme(disp, dark)` calls `lv_theme_default_init()` then `lv_display_set_theme(disp, theme)`. Called once right after `lv_display_create()` in `lvglInit()` (before any screen/widget is built, so everything starts under the right theme), and again from Config's new Light/Dark buttons to switch live. `lv_theme_default_init()` internally calls `lv_obj_report_style_change(NULL)` whenever the `dark` flag actually changes, which refreshes every existing object on every screen globally -- so toggling from Config immediately re-themes Main too, even though Main isn't the visible screen at the time.
- **Persisted via `Preferences`** (same library/pattern `wifi_setup.cpp` already uses for WiFi credentials): namespace `"display"`, key `"dark"`, loaded at the very start of `lvglInit()` (before `lv_init()`) so the saved choice applies from the very first boot screen, not just after a later toggle.
- **Config screen additions**: a "Display:" caption plus two buttons, "Light" and "Dark" -- LVGL has no built-in radio-button group, so this is a hand-rolled two-button exclusive choice (`updateThemeButtonStyles()` blue-highlights whichever is active, grey for the other; called once at boot to reflect the loaded preference, and again after every tap).
- **Deliberately doesn't affect everything**: the black temp-tile backgrounds and their colored numbers (`createReadoutTile()`) are drawn with explicit `lv_color_black()`/`lv_palette_main()` calls, independent of the theme -- they stay the same in both modes by design, matching the original "black tile, per user feedback" look from earlier in this branch. Only the generic theme-driven chrome (screen background, button/slider default colors, label text color) actually changes.

**Verified on hardware (COM6)**: build succeeded; reset-and-capture shows a clean boot log, no crash/exception, no reboot loop. **Visual confirmation that Light/Dark actually swap the Main screen's look, and that the choice survives a reboot, is still pending.**

### Open items for `lvgl-ui` branch
- Visually confirm on the physical device: Config's Light/Dark buttons actually change Main's background/chrome live, the active button is visibly highlighted, and the choice survives a power cycle (Preferences persistence).
- Everything else unchanged from the entries above.

## 2026-08-12 (continued) — `lvgl-ui` branch: splash screen fade-in/fade-out animation

User: "先用 lv_anim 做一些效果。 'Trident : WiFi BLE USB to Skywalker' 淡入 淺出" -- wanted the (previously static-text) splash screen to actually animate, using `lv_anim`, and to show a tagline alongside the "Trident" title summarizing what it bridges (WiFi/BLE/USB) to (a Skywalker roaster).

Splash screen replaced: previously just a single centered "Trident" label. Now:
- A transparent full-screen container (`splashContent`) holds two labels -- "Trident" (montserrat_32, same as before) and "WiFi  BLE  USB  to Skywalker" (default 14pt) as a tagline below it -- so both fade together as one unit instead of needing two animations kept in sync.
- Animated with LVGL's own built-in helpers, `lv_obj_fade_in(obj, time, delay)` / `lv_obj_fade_out(obj, time, delay)` (`src/core/lv_obj_style.h`) rather than a hand-rolled `lv_anim_t` -- these already do exactly "animate opacity 0→COVER" / "COVER→0" over `time` ms starting after `delay` ms, which is all a plain fade needs. Sequence: fade in over 500ms, hold fully visible for 800ms, fade out over 500ms (total 1800ms), then the existing one-shot splash timer (now driven by the same three constants instead of a separate hardcoded 1500ms) switches to Main right as the fade-out finishes.
- No flash of full opacity before the animation starts: `lv_obj_fade_in()` always animates from an explicit 0, not from the object's actual current opacity, and this all happens before the very first `lv_timer_handler()` tick (screen isn't even loaded yet), so there's no intermediate frame that could render it at full opacity first.

**Verified on hardware (COM6)**: build succeeded; reset-and-capture shows a clean boot log, no crash/exception, no reboot loop. **Visual confirmation of the actual fade-in/hold/fade-out on the physical screen is still pending.**

### Open items for `lvgl-ui` branch
- Visually confirm on the physical device: splash text fades in smoothly, holds, fades out smoothly, then Main appears right as it finishes (no flash/pop, no gap).
- Everything else unchanged from the entries above.

## 2026-08-12 (continued) — `lvgl-ui` branch: splash fade animation was invisible, real bug found and fixed

User reported after the previous round: "你有implement嗎？我沒有看到任何效果" (did you actually implement it? I don't see any effect at all) -- not "wrong timing" but genuinely nothing visible.

**Root cause, confirmed by reading LVGL's own source** (`lv_anim.c`'s `lv_anim_start()`): calling `lv_obj_fade_in()` immediately followed by `lv_obj_fade_out()` on the same object -- which is exactly what the previous round did -- doesn't queue two animations. `lv_anim_start()` calls `remove_concurrent_anims()` for any new animation with `early_apply` set (the default), which **deletes any existing animation already running on the same object+style-property** before adding the new one. So the moment `fade_out()` was created, it deleted `fade_in()`'s animation outright -- and because `early_apply` also means the new animation's start value gets written *synchronously* the instant it's created, `fade_out()` captured its own "start" opacity as whatever `fade_in()` had *just* set it to a moment earlier (0, from `fade_in`'s own early-apply). Net effect: the container animated 0→0 for the entire splash duration -- fully transparent, invisible, the whole time. Not a timing bug, a genuine "the effect never ran" bug.

**Fix**: don't create the fade-out animation until fade-in's animation has actually finished (and self-removed from LVGL's internal animation list) on its own. Replaced the back-to-back `fade_in()` + `fade_out()` calls with `fade_in()` alone, plus a separate one-shot `lv_timer` (`splashFadeOutCb`) scheduled for `kSplashFadeMs + kSplashHoldMs` that creates the `fade_out()` animation only once that timer fires -- by then fade-in's own animation is long gone from the list, so there's no concurrent-animation collision to trigger the delete-and-recapture bug. `splashContent` (previously a local variable) is now a static file-scope global so the new timer callback can reach it, matching the existing pattern for other screen-level objects.

**Verified on hardware (COM6)**: build succeeded; reset-and-capture shows a clean boot log, no crash/exception, no reboot loop. **Visual confirmation that the fade is now actually visible is still pending** (this fix hasn't been eyeballed on the physical screen yet either).

### Open items for `lvgl-ui` branch
- Visually confirm on the physical device: splash text is now actually visible and audibly (visually) fades in, holds, fades out -- not just present in code.
- Everything else unchanged from the entries above.

## 2026-08-26 — `et-max31865` branch: external ET probe (MAX31865 + PT100)

Branched from `lvgl-ui`. Adds a second, independent temperature channel (ET / exhaust) from an external MAX31865 RTD-to-digital converter + 4-wire PT100 probe. The roaster's own built-in probe still supplies BT over the serial protocol, unchanged.

**Wiring (done by user):** MAX31865 shares the touch controller's SPI bus rather than a new bus — VIN→3V3, GND→GND, SCLK→GPIO42, SDI→GPIO2, SDO→GPIO41 (all shared with XPT2046), CS→**GPIO40** (dedicated, `ET_CS_PIN` in [pindef.h](src/pindef.h), S3 only). Rationale in the chat plan: the touch bus is near-idle vs. the display bus's saturated pixel traffic, and both the touch read and the ET read run on the *same* task (see below), so the shared bus needs no lock.

**New files:**
- [src/ror.h](src/ror.h) / [src/ror.cpp](src/ror.cpp) — `RorTracker` class. The Artisan-matching ROR algorithm that used to be file-scope globals + `updateROR()` inside [SkiComms.h](src/SkiComms.h) (BT-only), pulled out so BT and ET run the *identical* calculation from separate instances instead of a hand-copied second copy that could drift. BT's instance (`btRor`) lives in SkiComms.h and still writes the global `ror`; ET's lives in et_sensor.cpp.
- [src/et_sensor.h](src/et_sensor.h) / [src/et_sensor.cpp](src/et_sensor.cpp) — the MAX31865 driver. Deliberately **does not** use the Adafruit_MAX31865 library: its `readRTD()` hard-codes `delay(10)+delay(65)` into every call, which would stall the LVGL/display task ~75ms per sample. Instead: set the config register once for VBIAS-on + continuous auto-convert + 60Hz filter + 4-wire, then each sample is just a 3-byte register read (tens of µs, no blocking). Callendar–Van Dusen conversion math is copied verbatim from that library. 5-wide median filter + a filtTemp()-style sanity gate reject the occasional corrupted SPI read. Exposes `etTemp`, `etRor`, `etSensorHealthy()`, `etReport()`. Non-S3 builds get no-op stubs (`etReport()` returns BT).

**Wired in:**
- `etSensorInit()` in `setup()` right after `touchInit()` (shares its already-begun `SPI.begin()`); `etSensorTick()` from `displayLoop()` right after `lvglLoop()` — same task as the LVGL touch-SPI reads, so the shared bus stays single-threaded with no mutex. Self-rate-limited to 250ms.
- **TC4 ET field** now carries the probe. `StateDataT` gained a `double et`; the READ reply builders in [SkiCMD.h](src/SkiCMD.h) `handleREAD()`, [main.cpp](src/main.cpp) `handleSerialCommand()`, [ble.cpp](src/ble.cpp) `buildReadMessage()`, and the WebSocket `root["data"]["ET"]` in [CommandLoop.cpp](src/CommandLoop.cpp) all now send `etReport()` in field 1 (ET) and BT in field 2 — matching `skywalker.aset`'s `arduinoETChannel=1` / `arduinoBTChannel=2`. `etReport()` falls back to mirroring BT when the probe is absent/faulted, so pre-probe behaviour is preserved.
- **Dashboard ET / ET RoR tiles** (previously hard-coded `"--.-"` placeholders on `lvgl-ui`) now show `etTemp` / `etRor`, or `"--.-"` when `!etSensorHealthy()`. `etLabel`/`etRorLabel` promoted to file-scope statics.

**Hardware bring-up (COM6, 2026-08-26):** flashed, clean boot (all 8 `[LVGL]` checkpoints, no crash/reboot loop). Added temporary USB-serial debug to `etSensorInit()`/`etSensorTick()` to diagnose (removed after).
- SPI comms confirmed: config register wrote `0xC0`, read back `0xC0`. `fault=0`, `faultstat=0x00` — probe wiring is fine.
- **Bug found + fixed: `ET_RREF` was 430.0, this board's reference resistor is 4300 ohm.** A room-temp PT100 read `rtd=844` → with RREF=430 that's `Rt=11 ohm` → `-217 C` (garbage, tripped the out-of-range gate → ET stayed `--.-`). With `RREF=4300.0` the same `rtd` gives `Rt=110.7 ohm` → `~28 C` (correct). It's a "PT100/PT1000 universal" red board — 4300 ohm reference so PT1000 doesn't overflow, at the cost of PT100 ADC resolution (~0.35 C/count vs ~0.035 C on a 430 ohm board). Fine for exhaust temp; median-7 filter (bumped from 5, matching BT) damps the raw ~±1 C count jitter.
- Post-fix: ET reads a stable ~27–29 C at room temp. USB serial re-verified clean (boot ROM + `[LVGL]` only, no `[ET]` contamination of the TC4 stream). **Dashboard ET / ET RoR tiles confirmed displaying correctly on the physical screen.**

**Build:** `pio run -e s3` SUCCESS (RAM 41.7%, Flash 25.1%).

### Open items for `et-max31865` branch
- Confirm ET shows up in Artisan on the ET curve (TC4/serial and WebSocket paths) during a real session.
- Sanity-check ET RoR against BT RoR behaviour once there's real probe movement (heat gun / roast).
- Consider a light moving-average on top of the median if ET still looks jumpy in Artisan (the 4300 ohm-reference resolution hit is the root cause; a 430 ohm-reference board would be the real fix).
